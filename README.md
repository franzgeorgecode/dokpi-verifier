# DOKPI Verifier

Public disposable GitHub Actions runner for DOKPI verification of self-hosted software.

The workflow accepts only a public GitHub repository URL and a bounded Dockerfile/Compose path. Candidate code receives no repository secrets. B12 records verification evidence but never promotes software automatically.